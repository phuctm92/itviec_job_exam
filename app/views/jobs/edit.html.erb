<main class="container">
  <%= link_to sanitize("&larr; Back to job"), job_path(@job) %>

  <div class="header">
    <h1>Edit Job</h1>
  </div>

  <%= turbo_frame_tag @job do %>
    <%= render "form", job: @job %>
  <% end %>
</main>
